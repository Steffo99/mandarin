"""
Apps are :class:`fastapi.FastAPI` instances with different capabilities: each one uses a different subset of
:ref:`Routes`, allowing access to different API methods.

Each one can be run independently from the other.
"""
